export enum BlockTypes {
    TASK = "task",
    REMINDER = "reminder",
    NOTE = "note",
    STANDUP = "standup",
    DAILY = "daily",
    HABITS = "habits"
}

export enum MatrixTypes {
    TODO = "todo",
    SCHEDULE = "schedule",
    DELEGATE = "delegate",
    DELETE = "delete"
}